using SafariPark.Models;

namespace SafariPark.Animals
{
    internal class Crocodile : Reptile
    {
        public Crocodile(string name, string separationOfAnimals, string color, int age)
            : base(name, separationOfAnimals, color, age)
        {
        }
    }
}
